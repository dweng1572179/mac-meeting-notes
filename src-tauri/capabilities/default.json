{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Meeting Notes.",
  "windows": ["main"],
  "permissions": ["core:window:default", "core:window:allow-destroy", "core:event:default"]
}
