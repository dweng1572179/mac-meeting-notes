# v0.5.0 validation receipt — 23 September 2026

Code checkpoint: `f215319` (later documentation-only changes do not alter this tested code). Release and installer receipts are updated below as they complete.

## Local automated checks

- `CARGO_BUILD_JOBS=2 cargo test --locked`: **179 passed, 5 ignored**. Includes synthetic long-recording rotation, recovery/checkpoint failures, exact previous-document recovery, concurrent edits, and platform-independent OpenAI contracts. A callback/rotation test initially failed because its producer could finish before AAC setup; deterministic batch barriers removed that timing assumption, then the focused and full checks passed.
- `cargo clippy --locked --all-targets -- -D warnings` and `cargo fmt --check`: passed.
- `npm test -- --run`: **13 API tests and 71 Vitest tests passed**. Includes conversation restart/navigation, scope isolation, retry, persistence failure, deletion, and preview parity.
- `npm run check`: zero errors and warnings. `npm run build`: passed.
- Opt-in `live_synthetic_meeting_questions_with_openai`: passed, three paid synthetic text-only requests in 5.9 seconds. Broad answer and contextual Spanish follow-up each resolved to two saved passages; an unavailable fact was handled without inventing it. No user notes or recordings were sent by this check. Other ignored live/device checks were not run.

## Browser acceptance

Exercised in the synthetic local preview, not a claim of native audio behavior:

- 1180×760 and minimum 900×620 windows: readable chat, no horizontal overflow.
- Forty-section answer renders completely; full answer survives navigation/reload. Copy reports success. Sources disclose exact passages and navigate to the meeting; returning retains the conversation.
- Thirty-second pending request survives meeting navigation; a different draft can be typed while waiting and remains after completion.
- Simulated connection failure retains the question; explicit retry succeeds. Reload during a pending request preserves prior answers and offers explicit retry without issuing a new request.
- Unicode drafts survive reload; ordinary Enter inserts a newline, Ctrl+Enter submits, and a composing Enter event does not submit. This checks the browser event guard, not a physical IME on every platform.
- Reduced-motion emulation disables meaningful transitions; empty/no-key states remain usable.
- Editing, updating, and restoring notes returns the exact synthetic wording. Native durability is separately covered by Rust storage tests.
- Browser reported no page errors. Browser and development-server processes were stopped afterward.

Synthetic UI evidence: [normal window](images/v0.5.0-conversation.png), [minimum window](images/v0.5.0-minimum-window.png).

## Review

Independent review found related-conversation deletion risks. Cleanup now precedes native deletion, failed cleanup blocks deletion, pending results are invalidated, and stored candidate-source membership covers in-flight responses. Unrelated histories and unsent drafts remain. Follow-up independent review reported no additional concrete blockers. This is a bounded review, not proof that defects cannot remain.

## Native packaging and release

- Mac local application build and DMG/signature/checkpoint checks: pending.
- Native Windows CI and installer preservation: pending.
- Dual-platform release and published checksums: pending.
- Installed Mac version and saved-data comparison: pending.

Physical Windows microphone/system loopback, Bluetooth, device removal/switching, sleep/wake, and real Edge playback are **unverified**. Hosted Windows already includes WebView2; a successful installer smoke test does not validate bootstrap on a clean PC missing that runtime. Windows remains an x64 beta; endpoints are selected at recording start, so restart capture after changing them.

Granola was inspected on a Business Trial account through its actual native interface using a synthetic note and built-in demo. Its actual long-recording, crash/offline recovery, speaker accuracy, first-sign-in flow, and account-restricted integrations were not validated. See the [comparison](granola-comparison-2026-09-23.md).
